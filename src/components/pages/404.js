import React from "react"

// import { useStyles } from "../Styles/StyleNotFound"

const NotFound = () => {
    // const classes = useStyles()

    return (
        <div>
            <h1>Page Non Trouvée</h1>
            <p>Oups ! Cette page n'existe pas !</p>
        </div>
    )
}

export default NotFound
